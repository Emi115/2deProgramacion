// utils/index.js

// Importa el objeto 'logger' desde el archivo 'logger.js'
import logger from './logger.js';

// Exporta el objeto 'logger' para que pueda ser utilizado desde otros archivos
export default logger;
