// Puerto en el que la aplicación escuchará las solicitudes.
const port = 3000;

// Exporta la configuración del puerto para que pueda ser utilizada en otros archivos.
module.exports = {
    port,
};
